server:
  port: 8090

jdac:
  baseUrl: http://localhost:8080/service/backlogic/example
  basePackage: net.backlogic.persistence.springboot.classic.repository
  logRequest: true
  jwtProvider:
    class: simple
    jwt: ""

logging:
  level:
    root: INFO
